# Рисование

Перед правкой `_try_gemini_image`, `_try_gpt_image`, `DRAW_MARKER_RE`, `LEAKED_DRAW_RE`,
`_draw_and_send`, `IMAGE_PROVIDERS`, `/imagine`, `/banana`, `/gptimage`, `/imagemodels` —
читай этот файл. История багов маркера и провайдеров с датами — `docs/claude/incidents.md`
(инциденты 2026-09-07 ×2, там же живёт полный разбор).

## Провайдеры и фоллбек

Реестр `IMAGE_PROVIDERS` в bot.py: `/banana` (Nano Banana 2, прямой Google API,
`GEMINI_API_KEY`) / `/gptimage` (**дефолт с 2026-09-19**, GPT Image 2, через пул
`api.apitoken.sale`, `ANTHROPIC_API_KEY`) — решение Алексея: banana дороже, GPT дешевле
(хоть и медленнее), по умолчанию платит более дешёвый канал. Явно выставленные строки
(в любую сторону) эта смена дефолта не трогает и не мигрирует. При отказе провайдер
переписывает промпт через Haiku и пробует
снова СВОИМ же провайдером — фоллбека между banana и gpt нет (разные счета за баланс,
молча подменять один другим значило бы незаметно для чата начать тратить деньги).
Фоллбека на FLUX тоже нет (выпилен: качество + FLUX.1-schnell стал gated, а годные
модели 2026 ушли с бесплатного hf-inference на платные провайдеры). При неудаче —
честная ошибка пользователю.

## Рисование: два пути входа
Вся генерация+отправка картинки инкапсулирована в `_draw_and_send(update, context, chat_id, is_group, draw_prompt, en_prompt=None, author=None)`. Если `en_prompt` не передан — `draw_prompt` переводится на английский через Haiku. Два способа запустить рисование:
1. **Команда «нарисуй …»** (`DRAW_TRIGGERS`) — явный запрос пользователя. Вызывает `_draw_and_send` с русским `draw_prompt`, переводом и автором.
2. **Маркер `[[DRAW: english prompt]]`** — Клодушка сама решает нарисовать внутри текстового ответа. В system-prompt ей сказано: НЕ писать «нарисовала» просто так (иначе картинка не появится), а добавлять маркер в конец ответа. `DRAW_MARKER_RE` парсит ответ в main conversation: маркер вырезается из текста (пользователь его не видит), остаток отправляется как реплика, затем `_draw_and_send` реально рисует. Если весь ответ был маркером — текст не шлётся, в группу пишет `_draw_and_send`. ЭТО ФИКС бага «Клодушка верит что нарисовала, но никуда не отправляет». Маркер обрабатывается только в текстовой ветке main conversation, не в фото/документах.
   Регекс лениво ловит 1 ИЛИ 2 скобки с обеих сторон, а если закрытия нет вообще —
   берёт до конца строки (`\[{1,2}DRAW:\s*(.+?)(?:\]{1,2}|$)`) — модель иногда сама
   сбивается на одинарную скобку без закрытия (`[DRAW: ...` до конца ответа), и старый
   строгий `\[\[DRAW:...\]\]` такое не ловил — маркер вываливался в чат текстом (v0.11.0).
   Известное ограничение: нелэйзи-стоп на первой `]` внутри промпта, если она там
   случайно есть — маловероятно для промптов на английском, не чинить, пока не встретится.

Защита от промпта-заглушки: если после вырезания всех не-буквенно-цифровых символов из
`draw_en_prompt` остаётся меньше 3 символов — считаем это промптом-заглушкой, рисование
пропускается (`draw_en_prompt = None`, warning в лог), картинка не шлётся и
`saved_text`/история это тоже не считают рисованием (место — сразу после
`DRAW_MARKER_RE.search(assistant_text)` в текстовой ветке main conversation, не в
фото/документах — там маркер и не парсится). Живой случай, приведший к этой защите —
инцидент 2026-09-07 в `docs/claude/incidents.md`.

Защитный fallback `LEAKED_DRAW_RE` (bot.py, рядом с `DRAW_MARKER_RE`): если настоящий
маркер не найден, но в ответе есть `нарисовал\w* картинк\w*[:\-—]`, а хвост после
разделителя преимущественно латиница (>60% букв ascii) и длиннее 15 символов — считаем
это утёкшим промптом, вырезаем этот хвост из видимого ответа и всё равно рисуем по нему
(`draw_en_prompt = candidate`). Требование «преимущественно латиница» — чтобы не
сработать на легитимных русских фразах вроде «не буду рисовать картинку: это тупо».
Это подстраховка НА СЛУЧАЙ повтора бага из инцидента 2026-09-08 (`incidents.md`), не
замена его первопричинного фикса (формат истории + system-prompt).

**Формат в истории `group_messages`/`conversations` — служебная пометка, не образец для
копирования.** Круглые скобки, никогда не начинается с «нарисовала» — так у обоих
провайдеров, обоих путей входа (команда/маркер) и **обоих контекстов, группа и личка**:
`(в чат отправлена картинка по промпту: <draw_prompt>)`. System-prompt явно объясняет
модели разницу: этот формат в истории — НЕ образец, чтобы нарисовать СЕЙЧАС нужен именно
`[[DRAW: ...]]`. Подробности, почему формат сменился именно так — инцидент 2026-09-08 в
`incidents.md`.

**До 2026-09-19 личка вообще не сохраняла факт рисования в `conversations`** (только
группа, через `_draw_and_send`) — реплай на нарисованную картинку терял весь контекст,
модель не понимала, о какой картинке речь. Починено (три места, все теперь единообразны):
- Явный путь «нарисуй …» — раньше вообще ничего не писал в личке (ни реплику
  пользователя, ни факт рисования). Теперь пишет пару `user`/`assistant` ВСЕГДА (даже
  при неудаче генерации — иначе одинокая `user`-реплика без ответа столкнулась бы со
  следующим сообщением пользователя и сломала бы чередование ролей, обязательное для
  API). При неудаче `assistant`-запись — `(попытка нарисовать картинку не удалась)`.
- Маркер `[[DRAW: ...]]` — плейсхолдер `(картинка отправлена)` раньше не содержал сам
  промпт (в отличие от группового аналога). Сработать `_draw_and_send`-подобной записью
  ЗДЕСЬ нельзя без риска второго `assistant` подряд (уже есть один — сохранённый ДО
  собственно рисования, `conversations` — плоский список, не транскрипт с ролями,
  считающимися отдельно от группы) — вместо этого сам плейсхолдер стал содержать промпт.
- **Реплай на фото, отправленное самим ботом** (`handle_message`, вычисление
  `reply_context`) — фото бот шлёт отдельным `reply_photo`, в текстовую историю/транскрипт
  оно не попадает как обычное сообщение, только caption. Раньше `reply_context`
  сознательно ИГНОРИРОВАЛ сообщения от самого бота (расчёт был — они и так в истории,
  для текстовых ответов это верно, для фото — нет). Теперь для фото от бота с caption —
  особый случай: `reply_context = "(в чат была отправлена картинка; подпись: {caption})"`.
  Работает и для группы, и для лички (в группе это дополнительная подстраховка сверх уже
  существующей записи в транскрипте — не мешает, просто избыточно на короткой дистанции).

**Система знает, что движок переключаемый.** System-prompt (`get_system_prompt`, общий
блок про рисование) явно говорит: бэкенд переключаемый, Клодушка не обязана знать, какой
активен именно сейчас, но не должна отрицать существование GPT Image 2 или утверждать,
что доступен только один вариант — до этого модель отвечала из общего знания и врала.
См. инцидент 2026-09-07 в `incidents.md`.

## Провайдер картинок per-chat (`/banana` `/gptimage` `/imagemodels`, v0.12.0)
Второй провайдер добавлен по образцу переключения моделей LLM (`MODELS`/`/haiku` и т.п.),
но это ОТДЕЛЬНЫЙ реестр и ОТДЕЛЬНАЯ таблица — картинки не через `messages.create`,
переиспользовать `chat_models`/`_set_chat_model` нельзя.

- **`IMAGE_PROVIDERS`** (bot.py) — `{"banana": {...}, "gpt": {...}}`, только `label`,
  без цен/окна/`admin_only` (решение сознательное: GPT Image 2 тратит общий баланс пула,
  но Алексей explicitly попросил не гейтить — доступно всем, кому доступно рисование
  вообще, referral+, как сейчас `/imagine`).
- **Хранение**: таблица `chat_image_provider(chat_id PRIMARY KEY, provider DEFAULT
  'gpt')`, `db.get_chat_image_provider_db`/`set_chat_image_provider_db`, обёртка
  `get_chat_image_provider(chat_id)` в bot.py — та же схема, что `chat_models`, но
  отдельная таблица: смешивать в одну означало бы городить составной ключ или второй
  столбец в чужой по смыслу таблице. **`db.DEFAULT_IMAGE_PROVIDER = "gpt"`** (модульная
  константа в db.py, не bot.py — тот же паттерн, что `DEFAULT_MODEL_ID`/`chat_models`:
  db.py ниже bot.py в зависимостях, не может его импортировать) — единственное место,
  откуда берётся и SQL `DEFAULT`, и Python-фолбэк в `get_chat_image_provider_db`. Было
  `'banana'` до 2026-09-19 (обратная совместимость на момент введения второго провайдера);
  сменилось на `'gpt'` решением Алексея — banana дороже. `CREATE TABLE IF NOT EXISTS`
  не пересоздаёт существующую таблицу — смена дефолта касается только новых установок
  и чатов, никогда не выставлявших `/banana`/`/gptimage`; явные выборы не мигрируют.
- **`_try_gpt_image(prompt)`** — сигнатура и семантика возврата (`image, error,
  provider_label`, `error == "__REFUSAL__"` при отказе) идентичны `_try_gemini_image`,
  чтобы `generate_image_with_error` могла звать оба провайдера единообразно.
  **GPT Image 2 идёт через пул `api.apitoken.sale`** (`POOL_BASE_URL`,
  `POST {POOL_BASE_URL}/v1/images/generations`, `{"model": "gpt-image-2", "prompt": ...,
  "n": 1, "size": "1024x1024"}`, заголовок `Authorization: Bearer ANTHROPIC_API_KEY` —
  тот же ключ, что и Claude/аудио, НЕ отдельный OpenAI-ключ). Проверено живым запросом
  с сервера 07.09.2026: 200, тело `{"data": [{"b64_json": "..."}]}` (только `b64_json`,
  без `url` — как у настоящего `gpt-image-1` в OpenAI). Ключ в заголовке, не в URL —
  как у `_transcribe_audio_gemini`. Идёт через `asyncio.to_thread` (не блокирует event
  loop) — тот же паттерн, что и у `_try_gemini_image`/`_transcribe_audio_gemini` (см.
  «блокирующие вызовы» в `docs/claude/api-errors.md` — раньше `_try_gemini_image` был
  исключением, теперь все три однотипны).
  **Не проверено живым отказом** (не нашли промпт, чтобы GPT Image отказался рисовать):
  модерация распознаётся эвристикой — 400 с "safety"/"moderation"/"policy" в тексте
  ошибки трактуется как отказ и уходит на переформулировку через Haiku, как у banana.
  Если эвристика не совпадёт с реальным форматом отказа пула — просто не сработает
  ретрай с переформулировкой, упадёт в общую ветку ошибки, не хуже, чем раньше.
  **`GPT_IMAGE_TIMEOUT = 120`** (не 60): инцидент 2026-09-07 сразу после первого
  деплоя — составной промпт («апокалипсис, ночь, еда, Debian») упал `ReadTimeout` ровно
  на 60с в проде; контрольный замер с сервера тем же промптом — 40.9с до 200. GPT Image 2
  заметно медленнее banana, старого потолка не хватало. `GPT_IMAGE_MAX_RETRIES` остался 1
  (как у banana) — ретрай уже упавшего по таймауту запроса удваивает и без того долгое
  ожидание, увеличенного per-request timeout достаточно. **Перепроверено при смене
  дефолта на gpt (2026-09-19)**: `120` — тот же потолок, что уже проверен живым запросом,
  менять не за чем. Индикатор «отправляет фото» (`_keep_chat_action`, см.
  `docs/claude/api-errors.md`) НЕ имеет собственного потолка длительности — тикает раз в
  4с, пока не придёт `stop_event`, поэтому переживает любую длину ожидания GPT Image
  автоматически, без отдельной настройки под конкретный провайдер.
- **`POOL_BASE_URL`** — переименован из `GEMINI_POOL_BASE_URL` (было использование только
  в аудио-транскрипции, теперь общий пул для Gemini-audio И GPT-image). У пула НЕТ модели
  генерации ЧЕРЕЗ GEMINI (banana там отдаёт 404, не путать) — GPT Image 2 отдельная,
  независимая модель пула под своим именем, не «банан через пул».
- **`generate_image_with_error(prompt, chat_id)`** — теперь принимает `chat_id`, выбирает
  провайдер через `get_chat_image_provider`, дальше диспетчеризует на `_try_gemini_image`
  или `_try_gpt_image`. Оба вызывающих места (`_draw_and_send`, `cmd_imagine`) передают
  `chat_id`, который у них уже есть.
- **`_set_image_provider(update, context, key)`** — команды `/banana`/`/gptimage`, по
  структуре как `_set_chat_model`, но БЕЗ пробного запроса перед записью: пробная
  генерация картинки стоит реальных денег и десятки секунд (не `max_tokens=1` на "hi").
  Без аргумента — текущий чат, всем; с `chat_id` — любой чат, только админу (та же логика
  защиты от «переключил модель в чужом чате», что у `/haiku` и т.п.).
  `/imagemodels [chat_id]` — список провайдеров с пометкой текущего, аналог `/models`.

## `_try_gemini_image` — не на пуле, ключ в заголовке

Рисование (`_try_gemini_image`) НА ПУЛ НЕ ПЕРЕЕХАЛО и остаётся на прямом Google API со
своим `GEMINI_API_KEY` — у пула нет модели генерации изображений вообще: запрос на
`gemini-3.1-flash-image-preview:generateContent` через `router.apitoken.sale` вернул
404 «is not found for API version v1beta, or is not supported for generateContent»
(проверено запросом 06.09.2026). Каталог моделей пула (`/md/models`) содержит только
текстовые Gemini-модели (flash/pro-preview) — если Google/пул когда-нибудь добавят
image-preview туда, переезд рисования стоит переоценить, но пока это тупик.

`GEMINI_API_KEY` уходит заголовком `x-goog-api-key`, не query-параметром `?key=` — как
и у `_transcribe_audio_gemini`/`_try_gpt_image`, единый паттерн для всех трёх (переведено
2026-09-19, `a9912bc`). Раньше ключ был в URL, а текст сетевого исключения `requests` иногда
содержал URL целиком — известный баг v0.8.1, закрыт архитектурно: секрета в URL больше
нет ни у одной из трёх функций. Вызов идёт через `await asyncio.to_thread(...)` — не
блокирует event loop (до 2026-09-19 блокировал, это было единственное исключение из
общего правила, см. `docs/claude/api-errors.md`).
